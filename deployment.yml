apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: httpd
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: httpd
    spec:
      containers:
      - name: httpd-web
        image: devalrajumahesh/kubernetes:tagname
        ports:
        - containerPort: 80
